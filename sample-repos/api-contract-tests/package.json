{
  "name": "api-contract-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:staging": "API_URL=https://staging.internal.example.com jest --runInBand",
    "wait-for-api": "node wait-for-api.js"
  },
  "devDependencies": {
    "@acme/schema-definitions": "^1.2.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
