# label-gen-cli

Pallet shipping label generator. Rust toolchain (no npm, no package.json): `cargo build --release`, `cargo test` runs unit tests plus e2e tests in `tests/` that exec the compiled binary via `CARGO_BIN_EXE`. No external crates.
