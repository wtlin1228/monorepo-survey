# stock-audit-cli

Stock CSV audit CLI. Python toolchain (no npm, no package.json): setuptools `pyproject.toml`, `python3 -m pytest` runs stdlib-only unit tests plus e2e tests that invoke the CLI as a subprocess. Install with `pip install .` (console script `acme-stock-audit`).
