# barcode-cli

Warehouse SKU barcode validate/generate CLI. Go toolchain (no npm, no package.json): `make build`, `go test ./...` for unit tests, `make e2e` for end-to-end tests that build and exec the real binary.
